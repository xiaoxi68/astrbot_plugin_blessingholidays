{
    "enabled": {
        "description": "是否启用插件",
        "type": "bool",
        "default": true
    },
    "llm_provider_id": {
        "description": "用于生成祝福的LLM提供商（从 AstrBot 已配置的提供商中选择）",
        "type": "string",
        "_special": "select_provider",
        "default": ""
    },
    "holidays_file": {
        "description": "节假日数据文件名",
        "type": "string",
        "default": "holidays.json"
    },
    "end_of_holiday_blessing": {
        "description": "假期结束提醒功能",
        "type": "object",
        "hint": "在假期的最后一天晚上发送“收假”提醒和祝福。",
        "items": {
            "enabled": {
                "description": "是否启用此功能",
                "type": "bool",
                "default": true
            },
            "send_time": {
                "description": "每日发送时间 (HH:MM)",
                "type": "string",
                "default": "22:00"
            }
        }
    },
    "start_of_holiday_blessing": {
        "description": "假期首日祝福发送设置",
        "type": "object",
        "items": {
            "send_time": {
                "description": "每日发送时间 (HH:MM)",
                "type": "string",
                "default": "00:05"
            }
        }
    }
}
